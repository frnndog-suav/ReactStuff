import { Box } from "@mui/material"
import React from "react"

export const Footer = () => {
    return (
        <Box sx={{ backgroundColor: "blue", color: "white" }}>
            this is a footer
        </Box>
    )
}