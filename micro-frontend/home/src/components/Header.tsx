import { Box } from "@mui/material"
import React from "react"

export const Header = () => {
    return (
        <Box sx={{ backgroundColor: "red",  color: "white" }}>
            this is a header
        </Box>
    )
}